# windows_startup_task

Consolidates `Register-SecretConLogonSeederTask` and SYSTEM startup tasks from bootstrap scripts.

**Parity step_ids:** psm-005, ews-014, asr-006, dc-005

**Used by:** `flags`, `defender_relax`, `asrep_promote`, `dc_promote`
