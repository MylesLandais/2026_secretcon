# asrep_promote

AD-DS feature, forest promotion, Kerberos/LDAP firewall, domain policy tuning.

**Parity step_ids:** asr-003–005, asr-010–016, asr-021

**Note:** `Install-ADDSForest` may remain PowerShell during Packer reboot (bootstrap-phase).
