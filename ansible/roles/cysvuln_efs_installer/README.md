# cysvuln_efs_installer

EFS Web Server 6.9 install, service account downgrade, option.ini, vfolders, firewall.

**Parity step_ids:** cyv-009–018, cyv-022, cyv-023

**Verify:** CysVuln deploy + `scripts/validate/*` AIE/EFS validators.
