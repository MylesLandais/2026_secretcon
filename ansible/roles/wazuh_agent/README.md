# wazuh_agent

Ports `Install-SecretConWazuhAgent` from `provisioning/powershell/lib/SecretCon.Bootstrap.psm1`.

**Parity step_ids:** psm-004, ews-003, cyv-003, asr-002, dc-002

**Verify:** Wazuh manager shows agent enrolled; `ossec.log` contains connected message.
