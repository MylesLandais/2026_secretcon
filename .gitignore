# Secrets
.env
*.pem
*.key
secrets/
.sops.yaml
provisioning/ssh/
!provisioning/ssh/readme.md
wazuh-creds-*.txt

# Vendored binaries (regenerable, not source)
provisioning/openssh/*.zip
provisioning/tightvnc/*.msi

# Local agent state — keep shared skill definitions under .claude/skills/
.claude/*
!.claude/skills/
output/

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Packer
packer_cache/
output-*/
*.pkrvars.hcl
!example.pkrvars.hcl

# Nix
result/
result

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Local build outputs and VM images
artifacts/
*.qcow2
*.vhdx
*.ova
*.vmdk
*.nvram
*.ovf
*.mf
__pycache__/
**/__pycache__/
*.pyc
.codex

# Personal NixOS configs (not part of this repo)
configuration.nix
home.nix
hardware-configuration.nix
hermes.nix
vars.nix
PORT.md.archived

# Generated PROVISION ISO for Hyper-V
infrastructure/packer/cysvuln/provision.iso
# Local ISO symlinks (stage via scripts/stage-cysvuln-iso.sh)
infrastructure/packer/iso/

# CysVuln challenge binaries (fetch via scripts/fetch-cysvuln-artifacts.sh)
infrastructure/artifacts/cysvuln/*.exe
infrastructure/artifacts/cysvuln/*.msi
!infrastructure/artifacts/cysvuln/.gitkeep

# Wazuh local-lab docker stack (generated certs + .env + named volumes)
infrastructure/wazuh-docker/.env
infrastructure/wazuh-docker/config/wazuh_indexer_ssl_certs/
infrastructure/wazuh-docker/data/

# SIEM capture loop run artifacts (randomized flags, drained alerts, etc.)
artifacts/cysvuln/observability-loop/

# AD Chain 8 - WIP reproduction of third-party (Hack Academy / OSCP) OVAs.
# Not a SecretCon-original challenge; keep off-tree until it earns its own
# box with red FAQ + blue FAQ + infra README + skill.
docs/ad-chain8/
docs/ad-chain8-audit-baseline.md
docs/ad-chain8-reproduction.md
docs/runbooks/forensics-ad-chain8.md
scripts/forensics/
scripts/ad-chain8/
scripts/run-chain8-dc.sh
scripts/run-chain8-lab.sh
scripts/run-chain8-lab-down.sh
infrastructure/sysmon/chain8-sysmon-delta.xml
infrastructure/wazuh-docker/config/wazuh_cluster/shared/chain8*/

# Bloodhound docker stack - WIP, currently scoped to the AD Chain 8 effort.
infrastructure/bloodhound-docker/
scripts/bloodhound-docker-up.sh
scripts/bloodhound-docker-down.sh

# Cursor process docs - planning history stays local
.cursor/plans/*.plan.md
