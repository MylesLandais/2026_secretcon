# Copy to `.env` (gitignored) and tune for non-defaults.
# Defaults below match the upstream demo creds; the stack only listens on
# localhost so they are acceptable for the SecretCon local lab.
INDEXER_PASSWORD=SecretPassword
API_PASSWORD=MyS3cr37P450r.*-
DASHBOARD_PASSWORD=kibanaserver
WAZUH_DASHBOARD_PORT=1443
