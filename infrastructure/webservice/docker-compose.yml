# STUB: player web-service stack (scoring UI only — no challenge provisioning).
# Replace with forked access broker + scoreboard when Tier B lands.
services:
  webservice-stub:
    image: alpine:3.20
    command: ["sh", "-c", "echo SecretCon webservice STUB — wire scoring + auth broker here; sleep infinity"]
    restart: "no"
    profiles: ["stub"]
